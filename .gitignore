# .gitignore

# --- General ---
# Logs and log directories
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables
# Ignore all .env files, but keep templates/examples
.env*
!.env.example
!.env.template

# --- Build & Output ---
# Compiled output directories common in JS/TS projects
dist/
build/
out/
public/build/
.next/
.nuxt/
.output/
.svelte-kit/
storybook-static/
.vite/
# Common cache dir (e.g., Remix, Gatsby)
.cache/
# WebAssembly binaries
*.wasm

# --- Dependencies ---
# Dependency directories (should stay ignored)
node_modules/
jspm_packages/
bower_components/

# Yarn Berry specific files (if using Yarn >= 2)
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
# Yarn PnP files
.pnp.*

# Dependency lock files (IMPORTANT: Usually should NOT be ignored)
# It is strongly recommended to commit lock files (package-lock.json, yarn.lock, pnpm-lock.yaml)
# to ensure reproducible builds. Only ignore them if you have a specific, unusual reason.
# Uncomment the relevant lines below ONLY if you have a strong reason to ignore lock files.
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# --- Testing ---
# Test coverage reports
coverage/
.nyc_output/
junit.xml
# Playwright test reports
playwright-report/
# Jest snapshots (optional: many teams commit these)
*.snap

# --- TypeScript ---
# TypeScript build cache files
*.tsbuildinfo

# Old TypeScript definition files (less common now)
typings/

# --- Caching ---
# Optional ESLint cache
.eslintcache
# Optional Prettier cache
.prettiercache
# Optional Stylelint cache
.stylelintcache
# Optional TurboRepo cache
.turbo/

# --- IDEs & Editors ---
# JetBrains IDEs (IntelliJ, WebStorm, etc.)
.idea/
*.iml

# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# Other Editors
*.sublime-workspace
*.suo
*.ntvs*
*.njsproj
*.sln
# Vim swap files
*.sw?

# VS Code history extension
.history/

# --- OS Generated Files ---
# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
# Custom icon files
Icon?

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# --- Miscellaneous ---
# Output of 'npm pack'
*.tgz

# Yarn Integrity file (less common now)
.yarn-integrity

# Compiled binary addons (if using native modules)
build/Release

# Temporary files
*.tmp
*~
*.temp

# --- Project Specific ---
# Add any files specific to *this* project that should be ignored
consolidated_output.txt
nestjs/src/prisma/generated
me/